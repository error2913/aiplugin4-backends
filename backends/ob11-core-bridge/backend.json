{
  "name": "ob11-core-bridge",
  "description": "SealDice 双连接冗余部署：协议端实时事件不转发核心（海豹另接直连协议端收发），核心 API 中转 + /plugin 注入假消息执行核心指令",
  "type": "node",
  "entry": "bridge.js",
  "deps": "package.json",
  "port": 46880,
  "version": "1.1.0",
  "config": {
    "protocol_url": {
      "label": "协议端 WebSocket 地址（如 ws://127.0.0.1:6700，中间件启动时主动连接）",
      "type": "text",
      "default": "",
      "env": "AIPLUGIN4_BRIDGE_PROTOCOL_URL"
    },
    "protocol_token": {
      "label": "协议端 token（可选，随地址一起发送）",
      "type": "text",
      "default": "",
      "env": "AIPLUGIN4_BRIDGE_PROTOCOL_TOKEN"
    }
  }
}
