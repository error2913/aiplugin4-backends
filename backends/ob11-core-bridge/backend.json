{
  "name": "ob11-core-bridge",
  "description": "SealDice 核心正向 WS /core、中间件主动连接的 OB11 协议端与 MCP /mcp 的透明中转；支持注入假消息执行核心/扩展指令",
  "type": "node",
  "entry": "bridge.js",
  "deps": "package.json",
  "port": 46880,
  "version": "1.0.4",
  "config": {
    "protocol_url": {
      "label": "协议端 WebSocket 地址（如 ws://127.0.0.1:6700，中间件启动时主动连接）",
      "type": "text",
      "default": "",
      "env": "AIPLUGIN4_BRIDGE_PROTOCOL_URL"
    },
    "protocol_token": {
      "label": "协议端 token（可选，随地址一起发送）",
      "type": "text",
      "default": "",
      "env": "AIPLUGIN4_BRIDGE_PROTOCOL_TOKEN"
    }
  }
}
